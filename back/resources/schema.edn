{:objects
 {:RawDeviceLog
  {:fields
   {:id {:type (non-null Int)}
    :data {:type String}
    :created_at {:type String}}}
  :RawDeviceLogs
  {:fields
   {:list {:type (list :RawDeviceLog)}
    :total {:type Int}}}
  :Query
  {:fields
   {:raw_device_logs
    {:type :RawDeviceLogs
     :args {:limit {:type Int}
            :offset {:type Int}
            :where {:type String}
            :order {:type String}}}}}}}
